# OLED de la mitad derecha (periferico):
# la animacion ahora es seleccionable en caliente (lower + click del
# encoder derecho). CAT=n para no duplicar los assets que ya compila el
# modo selectable.
CONFIG_NICE_OLED_WIDGET_ANIMATION_PERIPHERAL_CAT=n

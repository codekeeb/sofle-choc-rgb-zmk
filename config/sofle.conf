# Copyright (c) 2020 Ryan Cross
# SPDX-License-Identifier: MIT

CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y
# Disable built-in widgets to avoid conflicts
# CONFIG_ZMK_WIDGET_LAYER_STATUS is not set
# CONFIG_ZMK_WIDGET_BATTERY_STATUS is not set
# CONFIG_ZMK_WIDGET_OUTPUT_STATUS is not set
# CONFIG_ZMK_WIDGET_WPM_STATUS is not set
# CONFIG_ZMK_WIDGET_BATTERY_STATUS_SHOW_PERCENTAGE is not set

# Encoder support
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# Additional options
CONFIG_ZMK_SLEEP=y
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
# EXPERIMENTAL_CONN pone BT_CTLR_PHY_2M a n (ver app/Kconfig de ZMK), y eso
# es lo UNICO que cambia en esta version. Sin PHY 2M la radio va a 1 Mbps
# en vez de 2, lo que se nota como latencia al escribir: cada informe HID
# tarda el doble en salir. Se vuelve a activar a mano; el nRF52840 lo
# soporta y los hosts que no, negocian 1M igualmente.
CONFIG_BT_CTLR_PHY_2M=y

# Parametros de conexion BLE. Los defaults de ZMK (latency 30, timeout
# 400) ahorran bateria dejando que el teclado se salte hasta 30 eventos
# seguidos: con intervalos de 15 ms son 450 ms sin transmitir nada.
#
# Eso hace dos cosas malas cuando la senal se degrada al alejarse: la
# primera tecla tras una pausa tarda hasta 450 ms en salir, y durante
# ese silencio el enlace no se mantiene, asi que un fallo puntual tumba
# la conexion en vez de recuperarse.
#
# latency 10 -> 150 ms como mucho. timeout 500 -> 5 s de margen antes de
# dar la conexion por perdida. Cuesta algo de bateria; con 300 mAh y
# ZMK_IDLE_TIMEOUT a 60 s hay margen de sobra.
CONFIG_BT_PERIPHERAL_PREF_LATENCY=10
CONFIG_BT_PERIPHERAL_PREF_TIMEOUT=500

# ZMK v0.3 cambio el default de y->n (por clones sin inductor HV). Sin el
# high-voltage DCDC, VCC comes from the internal LDO and cannot power the
# 30 LEDs: they stay dark even though SPI data arrives fine.
CONFIG_BOARD_ENABLE_DCDC_HV=y

# RGB FX (replaces ZMK's stock underglow, which only has 4 effects).
# El underglow activaba SPI/LED_STRIP; ahora hay que activarlos a mano.
CONFIG_ZMK_RGB_UNDERGLOW=n
CONFIG_SPI=y
CONFIG_LED_STRIP=y
CONFIG_WS2812_STRIP=y
CONFIG_ZMK_RGB_FX=y
# Peripheral OLED animations switchable at runtime (&oledanim,
# fork codekeeb/zmk-nice-oled selectable branch). On both halves: the
# behavior tiene locality global.
CONFIG_NICE_OLED_WIDGET_ANIMATION_PERIPHERAL_SELECTABLE=y
# Battery as a graphic icon with fill (instead of the % number), both OLEDs.
CONFIG_NICE_OLED_WIDGET_BATTERY_GRAPHIC=y

# Pointing/mouse HID: needed for the &mwheel behavior (discrete scroll
# wheel on the encoders). Adds the mouse report to the HID descriptor, so
# the host may need re-pairing over BLE after this change.
CONFIG_ZMK_POINTING=y

# RGB auto-off: after a minute idle the effects stop and the strip
# apaga; reviven con la primera pulsacion (gestionado en src/rgb_fx.c).
CONFIG_ZMK_IDLE_TIMEOUT=60000

# Copyright (c) 2020 Ryan Cross
# SPDX-License-Identifier: MIT

CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y
# Deshabilitar widgets integrados para evitar conflictos
# CONFIG_ZMK_WIDGET_LAYER_STATUS is not set
# CONFIG_ZMK_WIDGET_BATTERY_STATUS is not set
# CONFIG_ZMK_WIDGET_OUTPUT_STATUS is not set
# CONFIG_ZMK_WIDGET_WPM_STATUS is not set
# CONFIG_ZMK_WIDGET_BATTERY_STATUS_SHOW_PERCENTAGE is not set

# Soporte para encoders
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# Opciones adicionales
CONFIG_ZMK_SLEEP=y
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y

# ZMK v0.3 cambio el default de y->n (por clones sin inductor HV). Sin el
# DCDC de alto voltaje, VCC sale del LDO interno y no da corriente para los
# 30 LEDs: quedan a oscuras aunque los datos SPI lleguen bien.
CONFIG_BOARD_ENABLE_DCDC_HV=y

# RGB FX (sustituye al underglow estandar de ZMK, que solo trae 4 efectos).
# El underglow activaba SPI/LED_STRIP; ahora hay que activarlos a mano.
CONFIG_ZMK_RGB_UNDERGLOW=n
CONFIG_SPI=y
CONFIG_LED_STRIP=y
CONFIG_WS2812_STRIP=y
CONFIG_ZMK_RGB_FX=y
# Animaciones OLED del periferico seleccionables en caliente (&oledanim,
# fork codekeeb/zmk-nice-oled rama selectable). En ambas mitades: el
# behavior tiene locality global.
CONFIG_NICE_OLED_WIDGET_ANIMATION_PERIPHERAL_SELECTABLE=y
# Bateria como icono grafico con relleno (en vez del numero %), ambas OLED.
CONFIG_NICE_OLED_WIDGET_BATTERY_GRAPHIC=y

# RGB auto-off: al minuto sin actividad los efectos paran y el strip se
# apaga; reviven con la primera pulsacion (gestionado en src/rgb_fx.c).
CONFIG_ZMK_IDLE_TIMEOUT=60000

# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#
---
include:
  - board: nice_nano_v2
    shield: sofle_left nice_oled
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y
  - board: nice_nano_v2
    shield: sofle_right nice_oled
  - board: nice_nano_v2
    shield: settings_reset
  # Debug: USB logs + solid red at boot + SPIM3 registers.
  - board: nice_nano_v2
    shield: sofle_left nice_oled
    snippet: zmk-usb-logging
    artifact-name: sofle_left_usb_logging
  # Debug: USB logs of the RIGHT half (split peripheral).
  - board: nice_nano_v2
    shield: sofle_right nice_oled
    snippet: zmk-usb-logging
    artifact-name: sofle_right_usb_logging
  # TEMP debug: Studio RPC + USB text logging together (two CDC-ACM ports),
  # to see LOG_* output while reproducing the oledanim set_layer_binding
  # INVALID_BEHAVIOR bug live. west -S takes a space-separated list in one
  # string, not a YAML sequence. Remove once diagnosed.
  - board: nice_nano_v2
    shield: sofle_left nice_oled
    snippet: "studio-rpc-usb-uart zmk-usb-logging"
    cmake-args: -DCONFIG_ZMK_STUDIO=y
    artifact-name: sofle_left_studio_and_logging
